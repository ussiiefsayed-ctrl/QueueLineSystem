# QueueLineSystem
QueueLine System (C++ Console Application) A console-based simulation of a queue management system built in C++. It handles ticket generation, serving clients, and tracking waiting/served customers. The project demonstrates the use of STL queue and stack, OOP concepts, and real-world queue behavior simulation. 
